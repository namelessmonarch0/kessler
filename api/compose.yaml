services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: leo
      POSTGRES_PASSWORD: leo
      POSTGRES_DB: leo
    ports:
      - "5432:5432"
    volumes:
      - leo-db:/var/lib/postgresql/data
volumes:
  leo-db:
