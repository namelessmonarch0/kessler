[project]
name = "kessler-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
  "psycopg[binary]>=3.2",
  "psycopg-pool>=3.2",
  "alembic>=1.13",
  "sqlalchemy>=2.0",
  "httpx>=0.27",
  "pydantic-settings>=2.4",
  "boto3>=1.43",
]

[dependency-groups]
dev = [
  "pytest>=8.3",
  "testcontainers[postgres]>=4.8",
  "respx>=0.21",
  "ruff>=0.6",
  "moto[s3,ssm]>=5.2",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
markers = [
  "pgbouncer: runs through a PgBouncer container in transaction mode (skipped if it cannot start)",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
ignore = ["E501"]  # long SQL/test lines; run `uv run ruff format` for layout

[tool.ruff.lint.flake8-bugbear]
# FastAPI's dependency-injection defaults are intentional.
extend-immutable-calls = ["fastapi.Depends", "fastapi.Query"]
