name: accuracy-tools
on:
  push:
    branches: [main]
    paths: ["tools/accuracy/**", ".github/workflows/accuracy-tools.yml"]
  pull_request:
    paths: ["tools/accuracy/**", ".github/workflows/accuracy-tools.yml"]
jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: tools/accuracy
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          python-version: "3.12"
      - run: uv sync --locked
      - run: uv run ruff check .
      - run: uv run pytest -q
